When using the bash diff command, use "rtk diff" instead.
When using the bash find command, use "rtk find" instead.
When using the bash grep command, use "rtk grep" instead.
When using the bash ls command, use "rtk ls" instead.
When using the bash read command, use "rtk read" instead.

When using the bash git diff command, use "rtk git diff" instead.
When using the bash git log command, use "rtk git log" instead.
When using the bash git pull command, use "rtk git pull" instead.
When using the bash git status command, use "rtk git status" instead.
